class TEST8

feature

	tata is 1 . 2 . 3;
	tata is 1 @ 2 @ 3;
	tata is 1 | 2 | 3;
	tata is 1 & 2 & 3;
	tata is 1 # 2 # 3;
--	tata is 1 ^ 2 ^ 3;
	tata is 1 * 2 * 3;
	tata is 1 / 2 / 3;
	tata is 1 // 2 // 3;
	tata is 1 \\ 2 \\ 3;
	tata is 1 + 2 + 3;
	tata is 1 - 2 - 3;
	tata is 1 = 2 = 3;
	tata is 1 /= 2 /= 3;
	tata is 1 < 2 < 3;
	tata is 1 <= 2 <= 3;
	tata is 1 > 2 > 3;
	tata is 1 >= 2 >= 3;
	tata is 1 and 2 and 3;
	tata is 1 and then 2 and then 3;
	tata is 1 or 2 or 3;
	tata is 1 or else 2 or else 3;
	tata is 1 xor 2 xor 3;
	tata is 1 implies 2 implies 3;

	tata is (1 + 2) + 3;
	tata is 1 + (2 + 3);

end