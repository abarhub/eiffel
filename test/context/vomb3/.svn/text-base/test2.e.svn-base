class TEST2

creation make

feature

	make is
	local
	c:CHARACTER;
	do
	inspect c
	when 'a','b' then
		cmd;
	else
		cmd;
	end;
	inspect c
	when 'a','b','A' then
		cmd;
	when 'B' then
		cmd;
	else
		cmd;
	end;
	inspect c
	when 'l','m','o','p','q' then
		cmd;
	when 'r'..'z' then
		cmd;
	else
		cmd;
	end;
	inspect c
	when 'l'..'q' then
		cmd;
	when 'a'..'k' then
		cmd;
	else
		cmd;
	end;
	inspect c
	when 'a'..'z' then
		cmd;
	when 'f'..'c' then
		cmd;
	else
		cmd;
	end;
	inspect c
	when 'a'..'m' then
		cmd;
	when t1,t2 then
		cmd;
	else
		cmd;
	end;
	end;

	t1:CHARACTER is 'A';
	t2:CHARACTER is 'y';

	cmd is
	do
	end;

end